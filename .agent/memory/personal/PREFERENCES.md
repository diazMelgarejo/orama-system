# Personal Preferences Pointer

Use `AGENTS.md`, `SKILL.md`, and `docs/LESSONS.md` as the durable source of
working preferences for this repo.

Project-local reminders:

- Be concise but include verification evidence.
- Preserve user changes and inspect diffs before staging.
- Prefer repo-relative paths and ASCII-only edits unless the file already uses
  non-ASCII intentionally.
- On Windows, inject real Git Bash and Python paths explicitly when tests need
  subprocess tools.
