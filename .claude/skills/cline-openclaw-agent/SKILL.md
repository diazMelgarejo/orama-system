---
name: cline-openclaw-agent
description: "Create or reconcile the dedicated OpenClaw Cline sub-agent and wire the Cline CLI as an agentic coding backend, including the ClinePass AutoResearcher fallback (Mac OpenClaw / Win Hermes gateway-down or rate-limited detection)."
---

<!-- THIN-WRAPPER: canonical skill lives in orama-system/bin/orama-system -->

# cline-openclaw-agent (thin wrapper)

Canonical, permanent implementation: `../../../bin/orama-system/skills/cline-openclaw-agent/`.
**Read it before proceeding** — this wrapper only carries discovery metadata.

ClinePass AutoResearcher fallback controller (the piece Mac/Win pulse cadence
calls when the native gateway is down or rate-limited): `../../../scripts/cline_autoresearcher.py`.
