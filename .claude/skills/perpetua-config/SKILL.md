---
name: perpetua-config
description: "This document outlines the model registry and device configuration for the ECC-tools ecosystem. Each device (Mac, Windows, shared Ollama) can run multiple backends (`ollama`, `mlx`, `lm-studio`), and each model is prioritized based on…"
---

<!-- THIN-WRAPPER: canonical skill lives in orama-system/bin/orama-system -->

# perpetua-config (thin wrapper)

Canonical, permanent implementation: `../../../bin/orama-system/skills/perpetua-config/`.
**Read it before proceeding** — this wrapper only carries discovery metadata.

Pre-wrapper body preserved at `SKILL.md.premerge-20260628.bak`.
