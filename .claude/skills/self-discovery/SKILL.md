---
name: self-discovery
description: "Query live state across all 3 repos — versions, branch status, LM Studio endpoints, file manifests. Run whenever you need a situational snapshot of the stack."
---

<!-- THIN-WRAPPER: canonical skill lives in orama-system/bin/orama-system -->

# self-discovery (thin wrapper)

Canonical, permanent implementation: `../../../bin/orama-system/skills/self-discovery/`.
**Read it before proceeding** — this wrapper only carries discovery metadata.

Pre-wrapper body preserved at `SKILL.md.premerge-20260628.bak`.
