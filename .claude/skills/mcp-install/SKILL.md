---
name: mcp-install
description: "Use when setting up the MCP orchestration stack on a new machine or verifying it is installed. Covers ai-cli-mcp (parallel background workers), the OpenClaw MCP registry, and an optional Gemini analyzer lane when explicitly requested…"
---

<!-- THIN-WRAPPER: canonical skill lives in orama-system/bin/orama-system -->

# mcp-install (thin wrapper)

Canonical, permanent implementation: `../../../bin/orama-system/skills/mcp-install/`.
**Read it before proceeding** — this wrapper only carries discovery metadata.

Pre-wrapper body preserved at `SKILL.md.premerge-20260628.bak`.
