---
name: perpetua-hardware
description: >-
  REDIRECT → hardware-affinity-gate. Gateway stub for Perpetua-Tools hardware
  affinity and routing — queries absorbed into the canonical hardware-affinity-gate
  skill. No procedure lives here.
version: 1.0.0
license: Apache 2.0
parent_skill: orama-system
---

# Perpetua Hardware

> **Redirected:** Hardware routing, model affinity, and dispatch rules are defined in
> [`../hardware-affinity-gate/SKILL.md`](../hardware-affinity-gate/SKILL.md).
>
> Perpetua-Tools `config/model_hardware_policy.yml` and
> `src/utils/hardware_policy.py` are the authoritative SSoT. This stub exists
> so links to `perpetua-hardware` resolve; load `hardware-affinity-gate` to act.

For local-inference endpoint wiring and LAN endpoint naming conventions see
[`../hermes-harness/references/lan-endpoint-contract.md`](../hermes-harness/references/lan-endpoint-contract.md).
