# Mac researcher — H4 Ollama 9B benchmark (branch)

**Assignee:** mac (mac-researcher, OpenClaw)  
**Topic:** autoresearch/gpu-run  
**Fan-out:** 2026-06-28-coord-003  
**Branch:** `subagent/mac-researcher/h4-mac-benchmark`

## Task

1. Read `mac-h4-synthesis.md` and Win `gpu-results-h4.md` (local inbox `read --name`).
2. Run same `clamp` coding prompt on Mac Ollama 9B (`localhost:11434`).
3. Write `mac-h4-comparison.md` with wall-clock vs Win ~33.1s.
4. Create branch `subagent/mac-researcher/h4-mac-benchmark` — optional small harness script under `bin/orama-system/skills/hermes-harness/scripts/` if needed.
5. Push branch; drop `mac-h4-comparison.md` + branch note to Win (`drop --peer`).

## Policy

See `subagent-branch-policy.md`.
