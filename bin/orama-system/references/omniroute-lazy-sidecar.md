# OmniRoute Lazy-Sidecar — MERGED / MOVED

> **Moved.** This reference was merged into the canonical OmniRoute sidecar skill:
> **[`../skills/omniroute/SKILL.md`](../skills/omniroute/SKILL.md)**
>
> That skill now carries the parallel-dispatch probe + workflow + NEVER rules (formerly here)
> **plus** the verified MCP-endpoint config, GUI password reset, and `storage.sqlite` map.
> Kept as a redirect so existing links resolve — do not add content here; edit the skill.
