# USER.md stub — Hermes profile operator overlay

Operator-specific preferences for this profile. **Not synced** from `bin/agents/` by default.

Installers copy this stub only on first profile create (`install_hermes_profiles.py --install`).

---

# Operator notes

- Preferred communication style:
- Escalation contacts:
- Schedule hooks:
