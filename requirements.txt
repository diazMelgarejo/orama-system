# orama-system v1.1.1.0 — Runtime dependencies
# Matches the usage block in api_server.py docstring.

# Web framework
fastapi>=0.109.0
starlette>=1.3.1
uvicorn[standard]>=0.27.0

# HTTP client (async, used by api_server.py _call_ollama)
httpx>=0.26.0

# LAN peer WS transport (lan_peer_channel.py, probe_lan_peer ws-peer check)
websockets>=12.0

# Config
python-dotenv>=1.2.2

# Data validation
pydantic>=2.6.0

# Security: rate limiting (OWASP API4)
slowapi>=0.1.9

# Test dependencies
pytest>=9.0.3
pytest-asyncio>=0.23.0
httpx>=0.26.0
