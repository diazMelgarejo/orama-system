# Codex reviewer agent role config
# Claude Code runtime: two install paths exist — see ../../CLAUDE-CODE-RUNTIME.md before touching the binary
[agent]
role = "reviewer"
read_only = true
description = "Read-only reviewer role for orama-system Codex multi-agent work."
