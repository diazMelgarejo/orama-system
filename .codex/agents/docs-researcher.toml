# Codex docs-researcher agent role config
# Claude Code runtime: two install paths exist — see ../../CLAUDE-CODE-RUNTIME.md before touching the binary
[agent]
role = "docs-researcher"
read_only = true
description = "Read-only docs-researcher role for orama-system Codex multi-agent work."
