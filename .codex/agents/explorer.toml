# Codex explorer agent role config
# Claude Code runtime: two install paths exist — see ../../CLAUDE-CODE-RUNTIME.md before touching the binary
[agent]
role = "explorer"
read_only = true
description = "Read-only explorer role for orama-system Codex multi-agent work."
