__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.DS_Store
**/.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
Icon?
*.log
.state/
PR_BODY.md
.env
.env.*
!.env.example
.env.local
.paths
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.jks
*.keystore
*.asc
*.gpg
*.age
*.kdbx
tokens/
secrets/
credentials/
auth/
*.token
*.secret
*.credentials.json
client_secret*.json
service-account*.json
google-credentials*.json
firebase-adminsdk*.json
.ecc
.ecc.bak
dist/
web/dist/
build/
out/
coverage/
.cache/
.tmp/
tmp/
temp/
DerivedData/
*.xcuserstate
*.xcscmblueprint
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserdatad/
.venv*/
*.whl
*.tar.gz
*.egg-info/
.idea/
.vscode/
.antigravity/
# macOS dedup (Finder "name 2" ghosts; worktree-bootstrap Datum D6)
*\ 2/
*\ 2.*
*\ 3/
*\ 3.*
* 2.md
* 2.py
* 3.md
* 3.py

# LM Studio auto-discovery (generated by discover.py)
.env.lmstudio

# Logs, databases, and UI capture artifacts
/logs/
*.log
*.log.*
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
*.db
*.sqlite
*.sqlite3
*.duckdb
*.lmdb
*.mdb
/data/
/runtime/
/state/
/tasks/
/sessions/
/screenshots/
/captures/
/ui-captures/
/recordings/
/playwright-report/
/test-results/
# Distillation run artifacts — contain session transcripts + trace JSONL (may contain prompt content)
docs/distill-fable-5/traces/
docs/distill-fable-5/runs/
*.jsonl
trace.zip
*.har
*.webm
*.mp4

# gbrain worktree pin — machine-local, set by /sync-gbrain (kubectl-style context)
# Different machines register under different source names; never commit this.
.gbrain-source

# discover.py runtime marker
config/.policy_cache_refreshed

# lib/ — agentic_stack and other generated/vendored shared libraries
# (content is managed externally; never commit here)
lib/
# Tracked install helpers (OpenClaw .mcp.json bootstrap for CRG)
!bin/orama-system/scripts/lib/
!bin/orama-system/scripts/lib/**

# Vite operator console (web/ subdirectory)
node_modules/
web/node_modules/
.vite/
*.tsbuildinfo
vite.config.d.ts
vite.config.js

# LanceDB runtime artifacts (generated by memory_store tests)
*.lance/
lance_memory.lance/

# Claude Code per-machine settings — contains personal absolute paths in
# permission allowlists. Tracked once accidentally; now gitignored (see
# repo_hygiene PERSONAL_PATH_PATTERN for the lint that catches re-commits).
.claude/settings.local.json

# Cursor private attribution (synced from ~/.cursor/openclaw — never commit)
.cursor/private/

# Accidental home-path mirror trees (never commit)
~/

# Runtime hook logs (never commit)
.claude/hooks/.logs/
